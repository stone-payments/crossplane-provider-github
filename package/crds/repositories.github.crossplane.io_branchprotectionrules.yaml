
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: branchprotectionrules.repositories.github.crossplane.io
spec:
  group: repositories.github.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - github
    kind: BranchProtectionRule
    listKind: BranchProtectionRuleList
    plural: branchprotectionrules
    singular: branchprotectionrule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A BranchProtectionRule is a managed resource that represents
          a GitHub Repository BranchProtectionRule configuration
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BranchProtectionRuleSpec defines the desired state of a BranchProtectionRule.
            properties:
              deletionPolicy:
                default: Delete
                description: DeletionPolicy specifies what will happen to the underlying
                  external when this managed resource is deleted - either "Delete"
                  or "Orphan" the external resource.
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: BranchProtectionRuleParameters defines the desired state
                  of a GitHub Repository BranchProtectionRule.
                properties:
                  bypassForcePushAllowances:
                    description: "Actors who may force push to the protected branch.
                      User, app, and team restrictions are only available for organization-owned
                      repositories. Defaults to disabled. \n Users should be specified
                      in the format: /{username}. Apps should be specified in the
                      format: /{app} Teams should be specified in the format: /{organization}/{team-slug}
                      NodeID should be specified in the format: {nodeId}"
                    items:
                      type: string
                    type: array
                  bypassPullRequestAllowances:
                    description: "A list of actors able to bypass PRs for this branch
                      protection rule. Defaults to disabled. \n Users should be specified
                      in the format: /{username}. Apps should be specified in the
                      format: /{app} Teams should be specified in the format: /{organization}/{team-slug}
                      NodeID should be specified in the format: {nodeId}"
                    items:
                      type: string
                    type: array
                  dismissesStaleReviews:
                    description: Whether new commits pushed to matching branches dismiss
                      pull request review approvals.
                    type: boolean
                  isAdminEnforced:
                    description: Whether admins can bypass branch protection rules.
                    type: boolean
                  owner:
                    description: The name of the Repository owner. The owner can be
                      an organization or an user.
                    type: string
                  pattern:
                    description: The pattern to be protected.
                    type: string
                  pushAllowances:
                    description: "Actors who may push to the protected branch. Defaults
                      to disabled. \n Users should be specified in the format: /{username}.
                      Apps should be specified in the format: /{app} Teams should
                      be specified in the format: /{organization}/{team-slug} NodeID
                      should be specified in the format: {nodeId}"
                    items:
                      type: string
                    type: array
                  repository:
                    description: The name of the Repository.
                    type: string
                  repositoryId:
                    description: Repository global node ID. If not specified, will
                      be infered by the Repository name field.
                    type: string
                  repositoryRef:
                    description: RepositoryRef references a Repository and retrieves
                      its name.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                    required:
                    - name
                    type: object
                  repositorySelector:
                    description: RepositorySelector selects a reference to a Repository.
                    properties:
                      matchControllerRef:
                        description: MatchControllerRef ensures an object with the
                          same controller reference as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                    type: object
                  requiredApprovingReviewCount:
                    description: Number of approving reviews required in the pull
                      request. Must be a number between 0-6.
                    format: int32
                    maximum: 6
                    minimum: 0
                    type: integer
                  requiredStatusCheckContexts:
                    description: List of required status check contexts that must
                      pass for commits to be accepted to matching branches.
                    items:
                      type: string
                    type: array
                  requiresCodeOwnerReviews:
                    description: Whether reviews from code owners are required to
                      update matching branches.
                    type: boolean
                  requiresCommitSignatures:
                    description: Whether commits are required to be signed.
                    type: boolean
                  requiresConversationResolution:
                    description: Whether conversations are required to be resolved
                      before merging.
                    type: boolean
                  requiresLinearHistory:
                    description: Whether merge commits are prohibited from being pushed
                      to this branch.
                    type: boolean
                  requiresStrictStatusChecks:
                    description: Whether branches are required to be up to date before
                      merging.
                    type: boolean
                required:
                - pattern
                type: object
              providerConfigRef:
                default:
                  name: default
                description: ProviderConfigReference specifies how the provider that
                  will be used to create, observe, update, and delete this managed
                  resource should be configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              providerRef:
                description: 'ProviderReference specifies the provider that will be
                  used to create, observe, update, and delete this managed resource.
                  Deprecated: Please use ProviderConfigReference, i.e. `providerConfigRef`'
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: WriteConnectionSecretToReference specifies the namespace
                  and name of a Secret to which any connection details for this managed
                  resource should be written. Connection details frequently include
                  the endpoint, username, and password required to connect to the
                  managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: BranchProtectionRuleStatus represents the observed state
              of a BranchProtectionRule.
            properties:
              atProvider:
                description: BranchProtectionRuleObservation is the representation
                  of the current state that is observed
                properties:
                  id:
                    description: Global ID that represents this BranchProtectionRule
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time this condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: A Message containing details about this condition's
                        last transition from one status to another, if any.
                      type: string
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: Type of this condition. At most one of each condition
                        type may apply to a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
            required:
            - atProvider
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
